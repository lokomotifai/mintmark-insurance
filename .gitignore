# Owner-only planning material. Never enters git history.
# Repository standard section 7.1: owner-only planning documents are gitignored
# by name.
PLAN.md
briefs/
research/
corpus/

# Reference datasets are release artifacts, never committed (FD-05). Only the
# bounded samples/ directory is.
dist/
*.jsonl
!samples/*.jsonl
!tests/**/*.jsonl

# Environments and caches
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS
.DS_Store
